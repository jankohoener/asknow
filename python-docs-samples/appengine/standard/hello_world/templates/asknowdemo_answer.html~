{% extends "asknowdemo.html" %}

{% block answer %}
	{% for answers in answerslist %}
		{% if answers['count'] == 1 }
		<p class="answer">The answer to your question "{{answers.question}}" is: <b>{{answers.title[0]}}</b></p><!-- FIXME: {{answer.title}} -->
		{% else %}
		<p class="answers">The answers to your question "{{answers.question}} are:
			{% for answer in answers.answers %} <!-- FIXME: needs nasty logic to do this -->
			
			{% endfor %}
		</p>
		{% endif %}
		<br>
		{% for answer in answers.answers %} <!-- FIXME -->
			<div class="information">
			<div class="abstract">Information on <i>{{answer.title}}</i> from Wikipedia:<br>
			{{answer.abstract | safe}}
			<span class="wplink">Read further information at <a href="{{answer.wplink}}">Wikipedia</a>.</span>
			</div>
			<div class="image">
			An image of {{answer.title}}:<br>
			<img src="{{answer.imgsrc}}" alt="An image of {{answer.title}}">
			</div>
			</div>
		{% endfor %}
	{% endfor %}
{% endblock %}